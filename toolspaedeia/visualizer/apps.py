from django.apps import AppConfig


class VisualizerConfig(AppConfig):
    """Configuration for the visualizer app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "visualizer"
