{
  "customizations": {
    "extensions": [
      "ms-python.python",
      "ms-python.vscode-pylance"
    ]
  },
  "dockerFile": "../Dockerfile",
  "forwardPorts": [
    8888
  ],
  "name": "Django Dev Container",
  "postCreateCommand": "uv pip install -r requirements.txt || uv pip install -e ."
}
